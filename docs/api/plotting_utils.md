# `plotting_utils`

PyVista rendering of scalar fields on a DOLFINx mesh. Rank-0 only — returns `None` on all
other MPI ranks.

```{eval-rst}
.. automodule:: plotting_utils
   :members:
   :undoc-members:
   :show-inheritance:
```
